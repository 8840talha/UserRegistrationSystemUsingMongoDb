# UserRegistrationSystemUsingMongoDb
A simple User regidtration form in node js and Mongo db as database with all validations by using the module express validator 
To run the app 
# Npm install and Node server.js
